import { storeCachedModules } from "../devicestorage/StorageController";
import { Module } from "../types/modules";

export async function getModule(module: Module, identiyId: string, token: string ) {
    
}