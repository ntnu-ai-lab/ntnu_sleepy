import React from "react";
import { View } from "react-native";
import { FormSection } from "../../types/modules";

export function FormSectionComponent(props: {
    section: FormSection
}) {

    return (
        <View>
            
        </View>
    )  
}